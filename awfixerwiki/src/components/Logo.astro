---
import { SITE } from "@/constants";

export interface Props {
  className?: string;
  alt?: string;
}

const { className = "", alt = "AWFixer Logo" } = Astro.props;
---

<img 
  src={SITE.LOGO_IMAGE} 
  alt={alt} 
  class={`logo ${className}`} 
/>