import React from "react";
import { GitHub, Twitter, RssIcon, XIcon } from "../components/Icons";
import { Link } from "../components/Link";
import { FOOTER_COLUMNS, FOOTER_QUOTE, FOOTER_INFO } from "@/constants";

export const Footer: React.FC = () => {
  const currentYear = new Date().getFullYear();

  return (
    <footer className="relative z-20 bg-black/80 backdrop-blur-md border-t border-white/10">
      <div className="container mx-auto px-8 py-12">
        <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-5 gap-8 mb-8">
          {FOOTER_COLUMNS.map(column => (
            <div key={column.title} className="space-y-4">
              <h3 className="text-lg font-semibold text-white mb-4">
                {column.title}
              </h3>
              <ul className="space-y-2">
                {column.links.map(link => {
                  const isExternal = link.href.startsWith("http");
                  return (
                    <li key={link.id}>
                      <a
                        href={link.href}
                        className="text-white/60 hover:text-white transition-colors duration-200 text-sm"
                        target={isExternal ? "_blank" : undefined}
                        rel={isExternal ? "noopener noreferrer" : undefined}
                      >
                        {link.text}
                      </a>
                    </li>
                  );
                })}
              </ul>
            </div>
          ))}
        </div>

        <div className="border-t border-white/10 pt-8">
          <blockquote className="text-center mb-8">
            <p className="text-white/80 italic text-lg md:text-xl font-serif">
              "{FOOTER_QUOTE.text}"
            </p>
            <footer className="text-white/60 text-sm mt-2">
              — {FOOTER_QUOTE.author}
            </footer>
          </blockquote>
        </div>

        <div className="border-t border-white/10 pt-8">
          <div className="flex flex-col md:flex-row items-center justify-between gap-4">
            <div className="flex items-center gap-4">
              <img
                src={FOOTER_INFO.logoUrl}
                alt={FOOTER_INFO.logoAlt}
                width={40}
                height={40}
                className="rounded-full border-2 border-white/20"
              />
              <div>
                <p className="text-white/60 text-sm">
                  © {currentYear} {FOOTER_INFO.companyName}
                </p>
                <p className="text-white/40 text-xs">All rights reserved</p>
              </div>
            </div>

            <div className="flex items-center gap-6">
              <p className="text-white/40 text-sm">{FOOTER_INFO.tagline}</p>
            </div>
          </div>
        </div>
      </div>
    </footer>
  );
};
